# Google OAuth (for user authentication flow)
# Create at: https://console.cloud.google.com/
# 1. Create new project
# 2. Enable Gmail API
# 3. Configure OAuth consent screen (External)
# 4. Create OAuth 2.0 Client ID (Web application)
# 5. Add redirect URI: http://localhost:8000/auth/google/callback
GOOGLE_CLIENT_ID=your_client_id_here
GOOGLE_CLIENT_SECRET=your_client_secret_here
GOOGLE_REDIRECT_URI=http://localhost:8000/auth/google/callback

# Search API (for calorie lookup)
# Get API key at: https://serpapi.com/
SERPAPI_KEY=your_serpapi_key_here

# Anthropic API (Claude for calorie extraction/estimation)
# Get API key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Database settings
# SQLite (local development):
#   DATABASE_URL=sqlite:///./swiggy_calories.db
#
# PostgreSQL (production):
#   DATABASE_URL=postgresql://user:password@host:5432/dbname
#
# Managed PostgreSQL services:
#   Supabase:  postgresql://postgres:[password]@db.[project].supabase.co:5432/postgres
#   Neon:      postgresql://[user]:[password]@[host].neon.tech/neondb?sslmode=require
#   Railway:   Use the DATABASE_URL provided by Railway
#   Render:    Use the Internal Database URL from Render dashboard
DATABASE_URL=sqlite:///./swiggy_calories.db

# Frontend URL (your Vercel deployment URL for production)
FRONTEND_URL=http://localhost:5173

# Secret key for JWT tokens (generate with: openssl rand -hex 32)
SECRET_KEY=your_secret_key_here_generate_with_openssl_rand_hex_32
